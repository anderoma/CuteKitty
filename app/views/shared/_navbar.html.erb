<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Cute Kitty</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      
      <li class="nav-item">
        <%= link_to "Les chatons du mois", chatons_path, class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Contact", contact_index_path, class:"nav-link" %>
      </li>
      <li class="nav-item active mr-4 pr-4" >
          <% if current_user == nil %>
            <%= link_to new_user_session_path do%>
              <i class="fas fa-shopping-cart fa-2x "></i>
            <% end%>
          <%else%>
            <%= link_to cart_path(current_user.id) do%>
              <i class="fas fa-shopping-cart fa-2x "></i>
            <% end %>
            <% if current_user.cart.items.length > 0 %>

             <span class="badge badge-pill badge-secondary">  <%= current_user.cart.items.length %></span>
           <% end %>
          <% end %>
        </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <% if current_user %>
        
			<button class="btn btn-link my-2 my-sm-0" type="submit"><%= link_to "Mon profil", user_path(:id => current_user.id), class:"nav-link" %></button>
				<button class="btn btn-link my-2 my-sm-0" type="submit"><%= link_to "Se déconnecter", destroy_user_session_path, method: :delete %></button>

    		<% else %>
        <button class="btn btn-link my-2 my-sm-0" type="submit"><%= link_to "Se connecter", new_user_session_path%></button>
        <button class="btn btn-link my-2 my-sm-0" type="submit"><%= link_to "S'inscrire", new_user_registration_path%></button>
      <% end %>
      
    </form>
  
  </div>
</nav>